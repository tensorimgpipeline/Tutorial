[project]
name = "level_5"
version = "0.1.0"
description = "Tutorial Level 5"
requires-python = ">=3.9"
dependencies = [
    "tensorimgpipeline",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"